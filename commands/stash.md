# stash

Stash the changes in a dirty working directory away  
`git stash` # same as git stash push

List  
`git stash list`

Inspect  
`git stash show`

Apply  
`git stahs apply`

Drop (last by one)  
`git stash drop`

